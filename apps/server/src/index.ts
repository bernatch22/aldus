// F7 — @aldus/server: composition root Node (bind IDocStore,
// IAgentEventSink→NdjsonSink), error middleware = UN catch site, rutas finas
// (ops → getAll(IInstantOp)), DocStore + GC de sesiones (TTL/LRU).
// Vacío hasta F7 (ver PLAN.md).
export {};

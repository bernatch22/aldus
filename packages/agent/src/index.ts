// F5 — @aldus/agent: transport/ (ILlmTransport + Claude SDK + OpenRouter),
// prompts, serialize, session/ (NodeIndex + EditSession fachada), runTurn,
// verify, tools, config, cli. Vacío hasta F5 (ver PLAN.md).
export {};

/**
 * @aldus/core/bake — Layer 3, EL CEREBRO (IEditApplier + PageBakeContext +
 * ILocator + FontService + BakeEvent). Vacío hasta F3; el subpath existe
 * desde F0 para fijar la forma pública del paquete.
 */
export {};
